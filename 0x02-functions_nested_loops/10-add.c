#include <stdio.h>
#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @n: first number
 * @m: second number
 * Return: result
*/
int add(int n, int m)
{
	int result;

	result = n + m;
	return (result);
}
